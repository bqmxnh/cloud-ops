ENV=production

# Model Configuration
# Auto-detect: Automatically load whichever model is in Production stage
# (Only 1 model can be in Production at a time)
MODEL_BUCKET=arf-ids-model-bucket
MODEL_VERSION=v1.0
AWS_DEFAULT_REGION=us-east-1
